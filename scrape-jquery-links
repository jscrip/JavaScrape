//This script depends jQuery & prep.js to be loaded first

//Scrape all links on the page. Return a CSV
var linkArray	= $("a").map(function(){
		return {
			text: $(this).text(),
			href: $(this).attr('href')
    }
})
downloadCSV(linkArray,"links");
